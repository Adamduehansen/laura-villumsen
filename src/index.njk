---
layout: layout.njk
title: Arbejde
metaDesc: Laura Villumsen, grafisk designer, med bopæl i København, og har primært fokus på visuelle identiteter, branding, typografi og design der er kælet for.
---

<div class="md:grid md:grid-cols-2 md:gap-3">
  {% for work in collections.works | reverse %}
    <div class="mb-6">
      <a href="{{ work.url }}">
        <img src="{{ work.data.thumbnail.url }}" alt="{{ work.data.thumbnail.alt }}" class="w-full"/>
        <div class="flex justify-between text-sm font-neue">
          <h2>
            {{ work.data.title }}
          </h2>
          <div>
            {{ work.data.publish_date }}
          </div>
        </div>
      </a>
    </div>
  {% endfor %}
</div>