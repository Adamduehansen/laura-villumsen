query WorkTeasers {
  posts(first: 15) {
    nodes {
      id
      uri
      featuredImage {
        node {
          sourceUrl
          altText
          mediaDetails {
            height
            width
          }
        }
      }
      workData {
        client
        frontpageText
        frontpageColor
      }
    }
  }
}
