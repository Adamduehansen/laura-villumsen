---
layout: layout.njk
---

<div class="flex overflow-x-auto whitespace-nowrap">
  {% for image in images %}
    <img src="{{ image.url }}" alt="{{ image.alt }}" class="max-h-full max-w-full object-contain mr-3 last:mr-0"/>
  {% endfor %}
</div>
{# {{ content | safe }} #}